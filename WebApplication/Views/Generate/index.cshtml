
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>View</title>
</head>
<body>
    <style type="text/css">

        .button {
            background-color: gray;
            border: none;
            color: white;
            padding: 15px 32px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
        }
    </style>
    @Html.ActionLink("Create Repo Etc", "CreateRepo", new { definitionId = 2 }, new { @class = "button" })

    @Html.ActionLink("Mass Release", "PerformMassRelease", new { definitionId = 2 }, new { @class = "button" })

    @Html.ActionLink("Create - Story - Done - API", "CreateStoryDoneAPI", new { definitionId = 2 }, new { @class = "button" })
</body>
</html>
